<footer class="mt-16 bg-black text-gray-400 border-t border-gray-800">
    <div class="mx-auto max-w-6xl px-6 py-6">
        <div class="text-center text-sm">
            <p class="font-semibold text-white">{{ site.org }}</p>
            <p class="mt-1">{{ site.department }}</p>
            <p class="mt-1 text-gray-500">{{ module_label | default("Journal Club Module") }}</p>
        </div>

        <div class="flex flex-col md:flex-row justify-between items-start md:items-center gap-4 pt-4 mt-4 border-t border-gray-800">
            <div class="flex flex-wrap gap-4 text-xs">
                <a href="#" class="hover:text-white transition-colors">Privacy Policy</a>
                <a href="#" class="hover:text-white transition-colors">Terms of Use</a>
                <a href="#" class="hover:text-white transition-colors">Contact</a>
            </div>
            <p class="text-xs text-gray-500">
                Copyright &copy; {{ page.date.getFullYear() if page.date else "2026" }} {{ site.org }}. All rights reserved.
            </p>
        </div>
    </div>
</footer>
